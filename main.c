#include <stdlib.h>
#include <stdio.h>

#include "lista_ligada.h"


int main() {

    

    return 0;
}

    double u = (double) ( random()+1 ) / RAND_MAX;
    double c = -( 1/lambda ) * log(u);
    
    double u = (double) ( random()+1 ) / RAND_MAX;
    double d = -dm * log(u);

